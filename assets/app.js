import * as Vue from 'vue';
import './app.scss';

/**
 * @description this file MUST remain empty! Only the top import should remain there.
 *              It's used to extract vendor/runtime js, and cannot contain any additional logic at all
 *              The import is required to enforce webpack to create vendor.js for this chunk where vendor.js
 *              contain common logic for all chunks, this is just simply how webpack works.
 */
